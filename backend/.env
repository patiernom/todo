# Local fallback DATABASE_URL for Postgres (Docker Compose overrides this)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/todos?schema=public"

# Server configuration
PORT=3000
HOST=0.0.0.0
ROUTE_PREFIX=""
