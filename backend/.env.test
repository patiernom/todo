# Integration test database environment
# Uses Docker Postgres service hostname "db" and an isolated schema named "test"
DATABASE_URL="postgresql://postgres:postgres@db:5432/todos?schema=test"

# Optional server settings (init doesn't bind a port, but harmless to keep)
PORT=3000
HOST=0.0.0.0
ROUTE_PREFIX=""
